name: Review Remediation

# When an AI review (code or security) fails on any trusted PR, run the agent
# to address the findings and push fixes to the same branch.
# Trusted = opened by app/claude OR carries the agent-ok label (maintainer opt-in).
#
# The PR re-enters the normal CI + review cycle once the fixes are pushed.
# Capped at MAX_REMEDIATION_ATTEMPTS per PR (default 3), counted by commits
# whose message starts with "fix: address automated review findings" — if the
# review still fails after the cap, a human must intervene.
#
# NOTE: workflow_run only fires from the default branch, so this workflow takes
# effect only after it is merged to main.

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  remediate:
    if: |
      github.event.workflow_run.conclusion == 'failure' &&
      github.event.workflow_run.head_repository.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    env:
      GH_TOKEN: ${{ github.token }}
      REPO: ${{ github.repository }}
      HEAD_BRANCH: ${{ github.event.workflow_run.head_branch }}
      MAX_REMEDIATION_ATTEMPTS: "3"
    steps:
      - name: Find PR and check remediation cap
        id: pr
        run: |
          set -euo pipefail

          # Only remediate when the review or security job failed, not core/app CI.
          review_failed=$(gh api "repos/$REPO/actions/runs/${{ github.event.workflow_run.id }}/jobs" \
            --jq '[.jobs[] | select(.conclusion == "failure") | .name] | map(select(. == "review" or . == "security")) | length')
          if [ "${review_failed:-0}" -eq 0 ]; then
            echo "CI failure is from core/app jobs, not review/security — skipping remediation."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          pr_number=$(gh pr list --repo "$REPO" --head "$HEAD_BRANCH" \
            --json number --jq '.[0].number // ""')
          if [ -z "$pr_number" ]; then
            echo "No open PR for branch $HEAD_BRANCH — skipping remediation."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "pr=$pr_number" >> "$GITHUB_OUTPUT"

          # Only remediate trusted PRs: opened by the claude bot OR carrying agent-ok.
          # gh uses GraphQL; app slug is "app/claude", label check covers maintainer opt-in.
          pr_author=$(gh pr view "$pr_number" --repo "$REPO" \
            --json author --jq '.author.login')
          has_ok=$(gh pr view "$pr_number" --repo "$REPO" --json labels \
            --jq '[.labels[].name] | contains(["agent-ok"]) | if . then "true" else "false" end')
          if [ "$pr_author" != "app/claude" ] && [ "$has_ok" != "true" ]; then
            echo "PR #$pr_number not trusted (author: $pr_author, agent-ok: $has_ok) — skipping remediation."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Allow up to 3 automated remediation attempts, counted by commits
          # whose message starts with the fixed remediation prefix.
          prior=$(gh api "repos/$REPO/commits?sha=$HEAD_BRANCH&per_page=100" \
            --jq '[.[] | select(.commit.message | startswith("fix: address automated review findings"))] | length')
          attempt=$((prior + 1))
          if [ "$attempt" -gt "$MAX_REMEDIATION_ATTEMPTS" ]; then
            echo "Remediation already attempted $MAX_REMEDIATION_ATTEMPTS times for PR #$pr_number — skipping."
            gh pr comment "$pr_number" --repo "$REPO" \
              --body "⚠️ Automated remediation has run $MAX_REMEDIATION_ATTEMPTS times and the review still fails. Manual intervention required." \
              || true
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "attempt=$attempt" >> "$GITHUB_OUTPUT"
          echo "skip=false" >> "$GITHUB_OUTPUT"

      - name: Generate App token
        # Push the fix commit as a GitHub App (not GITHUB_TOKEN) so that the
        # resulting pull_request.synchronize event is NOT gated by GitHub's
        # loop-prevention rule (which only applies to GITHUB_TOKEN pushes from
        # workflow_run-triggered jobs). This lets CI, Claude PR Review, and
        # Security Review re-trigger automatically with no manual intervention.
        # Requires APP_ID and APP_PRIVATE_KEY repo secrets (see docs/agentic-workflow.md).
        if: steps.pr.outputs.skip != 'true'
        id: app-token
        uses: actions/create-github-app-token@v3.2.0
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - uses: actions/checkout@v7
        if: steps.pr.outputs.skip != 'true'
        with:
          ref: ${{ github.event.workflow_run.head_branch }}
          fetch-depth: 0
          # Use the App token so git remote is configured with App credentials;
          # the agent's git push then authenticates as the App (not GITHUB_TOKEN).
          token: ${{ steps.app-token.outputs.token }}

      - uses: subosito/flutter-action@v2
        if: steps.pr.outputs.skip != 'true'
        with:
          channel: stable
          cache: true

      - name: Mark remediation in progress
        if: steps.pr.outputs.skip != 'true'
        run: |
          gh label create "agent-remediation" --repo "$REPO" \
            --color "e99695" \
            --description "Automated review remediation attempted on this PR" \
            2>/dev/null || true
          gh pr edit "${{ steps.pr.outputs.pr }}" --repo "$REPO" \
            --add-label "agent-remediation" || true

      - name: Post remediation started comment
        if: steps.pr.outputs.skip != 'true'
        id: start_comment
        run: |
          pr="${{ steps.pr.outputs.pr }}"
          run_url="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          cat > "$RUNNER_TEMP/start_body.txt" << 'BODY_EOF'
          🤖 **Automated remediation started (attempt ${{ steps.pr.outputs.attempt }}/${{ env.MAX_REMEDIATION_ATTEMPTS }}).** Addressing failing review findings — this comment will be updated with a summary when done.
          BODY_EOF
          printf '\n[View live progress →](%s)' "$run_url" >> "$RUNNER_TEMP/start_body.txt"
          comment_id=$(gh api "repos/$REPO/issues/$pr/comments" \
            --method POST \
            --raw-field body=@"$RUNNER_TEMP/start_body.txt" \
            --jq '.id' 2>/dev/null || echo "")
          echo "comment_id=${comment_id}" >> "$GITHUB_OUTPUT"

      - name: Fix review findings
        if: steps.pr.outputs.skip != 'true'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # App token so claude-code-action's internal git push is also App-authenticated.
          github_token: ${{ steps.app-token.outputs.token }}
          claude_args: "--permission-mode bypassPermissions"
          allowed_bots: "github-actions[bot],claude[bot]"
          prompt: |
            PR #${{ steps.pr.outputs.pr }} in ${{ github.repository }} failed an
            automated review. Your task: fix every blocking finding and push the
            fixes to the existing branch (do NOT open a new PR).

            1. Fetch all unresolved review threads — this gives you both the findings
               to fix and the node IDs needed to resolve them afterward:
                 gh api graphql \
                   -f query='query($owner:String!,$repo:String!,$pr:Int!){repository(owner:$owner,name:$repo){pullRequest(number:$pr){reviewThreads(first:50){nodes{id isResolved comments(first:5){nodes{body path line author{login}}}}}}}}' \
                   -f owner="${REPO%%/*}" -f repo="${REPO##*/}" \
                   -F pr=${{ steps.pr.outputs.pr }}

            2. Address every BLOCKING finding — this includes:
               - Correctness bugs and missing edge cases
               - Parity Rulebook violations (engineering/decisions/design-system.md)
               - Phase-1 constraint breaches (core must stay pure Dart, no Phase-2 entities)
               - Missing tests for new core computation
               - CRITICAL or HIGH security issues

            3. Delegate to subagents where appropriate:
               - `spec-auditor` to verify Parity Rulebook fixes
               - `test-author` to add or fix core unit tests

            4. Run the definition-of-done checks until all pass:
                 (cd flutter/packages/core && dart format --output=none --set-exit-if-changed . && dart analyze --fatal-infos && dart test)
                 (cd flutter && dart format --output=none --set-exit-if-changed . && flutter analyze && flutter test)

            5. Push the fixes to the existing branch.
               Use commit message: "fix: address automated review findings"
               Do NOT edit any file under `.github/workflows/` — workflow
               changes require human review and are out of scope for automated
               remediation.

            6. For each blocking finding you fixed, resolve its thread using the node
               ID from step 1 (best-effort — must NOT stop your work; use `|| true`):
                 gh api graphql \
                   -f query='mutation($id:ID!){resolveReviewThread(input:{threadId:$id}){thread{isResolved}}}' \
                   -f id="<THREAD_NODE_ID>" || true

            7. Write a markdown summary of the remediation to
               $RUNNER_TEMP/remediation_summary.txt covering ALL findings from
               step 1, grouped into two sections:

               **Addressed** (blocking findings you fixed — one bullet each)
               **Not addressed** (everything else — note the finding and why it
               was left for human review, e.g. non-blocking, needs a product
               decision, out of scope for automated remediation)

               Example:
               **Addressed**
               - Fixed: `FormatService.formatVolume` returned wrong unit label in imperial mode (line 42)
               - Fixed: Added unit test for 0 kg edge case in hydration goal calculation

               **Not addressed**
               - Low: variable name `v` is not descriptive — style nit, non-blocking
               - Medium: consider extracting helper — design preference, left for human review

      - name: Update remediation comment
        if: always() && steps.pr.outputs.skip != 'true' && steps.start_comment.outputs.comment_id != ''
        run: |
          comment_id="${{ steps.start_comment.outputs.comment_id }}"
          run_url="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          if [ -f "$RUNNER_TEMP/remediation_summary.txt" ]; then
            printf '🤖 **Automated remediation complete.** [View run →](%s)\n\n' "$run_url" > "$RUNNER_TEMP/update_body.txt"
            cat "$RUNNER_TEMP/remediation_summary.txt" >> "$RUNNER_TEMP/update_body.txt"
          else
            cat > "$RUNNER_TEMP/update_body.txt" << BODY_EOF
          ⚠️ **Automated remediation failed.** The fix step did not complete successfully — manual intervention may be required. [View run →]($run_url)
          BODY_EOF
          fi

          gh api "repos/$REPO/issues/comments/$comment_id" \
            --method PATCH \
            --raw-field body=@"$RUNNER_TEMP/update_body.txt" || true

