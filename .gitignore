# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock is ignored for now (regenerated from pinned SDK + constraints).
# When the app stabilises, consider committing flutter/pubspec.lock for
# reproducible builds (Dart convention: commit it for apps, ignore for packages).
pubspec.lock
coverage/
*.g.dart.lock

# Generated code is committed conventionally for Drift, but build artefacts are not.

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Local Claude settings (per-user, not shared)
.claude/settings.local.json
.claude/worktrees/
